<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Product Page{% endblock %}</title>

    <!-- Link to your CSS file -->
    <link href="{{ 'product-styles.css' | asset_url }}" rel="stylesheet" type="text/css">

    {% block head %}
    <!-- Additional head elements can be added here -->
    {% endblock %}
</head>
<body>
    {% block content %}
    <!-- Content will be injected here from product.liquid -->
    {% endblock %}

    <!-- Include JavaScript for Add to Cart functionality -->
    <script src="/script.js"></script>
</body>
</html>
